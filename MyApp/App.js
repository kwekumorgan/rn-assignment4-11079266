import React from 'react';
import LoginPage from './LoginPage';  // Adjust the path if LoginPage is in a different folder

const App = () => {
  return (
    
    <LoginPage />
  );
};

export default App;
